---
import type { Lang } from '../i18n/ui';
import { useTranslations } from '../i18n/ui';

interface Props {
  lang: Lang;
  nickname: string;
}

const { lang, nickname } = Astro.props;
const t = useTranslations(lang);

const otherLangHref = lang === 'en' ? '/id/' : '/';
const otherLangLabel = t('lang.switchTo');

const navItems = [
  { href: '#about', label: t('nav.about') },
  { href: '#skills', label: t('nav.skills') },
  { href: '#experiences', label: t('nav.experiences') },
  { href: '#projects', label: t('nav.projects') }
];
---

<header class="sticky top-0 z-50 border-b border-panel-border bg-bg/85 backdrop-blur">
  <div class="mx-auto flex max-w-5xl items-center justify-between px-6 py-4">
    <a href="#" class="text-sm font-semibold sm:text-base">
      <span class="text-prompt">{nickname.toLowerCase()}</span><span class="text-term-dim">@site</span><span
        class="text-prompt">:~$</span
      >
    </a>

    <nav class="hidden items-center gap-6 text-sm text-term-dim sm:flex">
      {
        navItems.map((item) => (
          <a href={item.href} class="transition-colors hover:text-prompt">
            {item.label}
          </a>
        ))
      }
    </nav>

    <div class="flex items-center gap-3">
      <a
        href={otherLangHref}
        class="rounded-md border border-panel-border px-2 py-1 text-xs text-term-dim transition-colors hover:text-prompt"
      >
        {otherLangLabel}
      </a>

      <button
        id="theme-toggle"
        type="button"
        aria-label={t('theme.toggle')}
        class="grid h-8 w-8 place-items-center rounded-md border border-panel-border text-term-dim transition-colors hover:text-prompt"
      >
        <svg id="icon-sun" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" class="hidden h-4 w-4 dark:block">
          <circle cx="12" cy="12" r="4" /><path d="M12 2v2M12 20v2M4.93 4.93l1.41 1.41M17.66 17.66l1.41 1.41M2 12h2M20 12h2M6.34 17.66l-1.41 1.41M19.07 4.93l-1.41 1.41" />
        </svg>
        <svg id="icon-moon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" class="block h-4 w-4 dark:hidden">
          <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79Z" />
        </svg>
      </button>

      <button
        id="nav-toggle"
        type="button"
        aria-label="Toggle navigation menu"
        aria-expanded="false"
        class="grid h-8 w-8 place-items-center rounded-md border border-panel-border text-term-dim transition-colors hover:text-prompt sm:hidden"
      >
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" class="h-4 w-4">
          <path d="M4 6h16M4 12h16M4 18h16" />
        </svg>
      </button>
    </div>
  </div>

  <nav id="mobile-nav" class="hidden space-y-1 border-t border-panel-border px-6 py-3 text-sm text-term-dim sm:hidden">
    {
      navItems.map((item) => (
        <a href={item.href} class="block rounded-md px-2 py-2 transition-colors hover:bg-panel hover:text-prompt">
          {item.label}
        </a>
      ))
    }
  </nav>
</header>

<script>
  const toggle = document.getElementById('theme-toggle');
  toggle?.addEventListener('click', () => {
    const isDark = document.documentElement.classList.toggle('dark');
    localStorage.setItem('theme', isDark ? 'dark' : 'light');
  });

  const navToggle = document.getElementById('nav-toggle');
  const mobileNav = document.getElementById('mobile-nav');
  navToggle?.addEventListener('click', () => {
    const isOpen = mobileNav?.classList.toggle('hidden') === false;
    navToggle.setAttribute('aria-expanded', String(isOpen));
  });
  mobileNav?.querySelectorAll('a').forEach((link) => {
    link.addEventListener('click', () => mobileNav.classList.add('hidden'));
  });
</script>
